<UserControl
    x:Class="Magentaize.FluentPlayer.Views.Common.PlaybackInfoCover"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Magentaize.FluentPlayer.Views.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:common="using:Magentaize.FluentPlayer.ViewModels.Common"
  xmlns:controls="using:Magentaize.FluentPlayer.Controls"
  mc:Ignorable="d">

    <Border>
      <controls:SlideTile Height="120" Width="120"
                          Duration="0:0:0.5"
                          Content="{x:Bind ViewModel.Thumbnail, Mode=OneWay}"
                          Direction="{x:Bind ViewModel.Direction, Mode=OneWay}"
                          >
      <controls:SlideTile.DefaultContent>
        <Image Source="ms-appx:///Assets/LockScreenLogo.scale-200.png"/>
      </controls:SlideTile.DefaultContent>
      <controls:SlideTile.ContentTemplate>
        <DataTemplate x:DataType="common:PlaybackInfoCoverThumbnailViewModel">
          <Image Source="{x:Bind Uri}"/>
        </DataTemplate>
      </controls:SlideTile.ContentTemplate>
    </controls:SlideTile>
  </Border>
</UserControl>
