<UserControl
    x:Class="Magentaize.FluentPlayer.Views.AlbumListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Magentaize.FluentPlayer.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Magentaize.FluentPlayer.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Border>
    <GridView ItemsSource="{x:Bind Vm.Albums, Mode=OneWay}">
      <GridView.ItemTemplate>
        <DataTemplate x:DataType="viewModels:AlbumViewModel">
          <Image Source="{x:Bind AlbumCoverFsPath, Mode=OneWay}"/>
        </DataTemplate>
      </GridView.ItemTemplate>
    </GridView>
    </Border>
</UserControl>
